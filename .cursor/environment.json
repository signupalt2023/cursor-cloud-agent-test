{
  "install": "source <(curl -fsSL \"${CURSOR_SETUP_REPO_URL:?Error: CURSOR_SETUP_REPO_URL not set in Cursor Secrets}/setup.sh\") && npm install",
  "terminals": [
    {
      "name": "Dev Server",
      "command": "npm run dev"
    }
  ]
}